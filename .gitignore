# Ignore local Claude Code state (settings.local.json, agent-memory, plans,
# scheduled tasks) but track shared, repo-tracked skills under .claude/skills/.
/.claude/skills/*
!/.claude/skills/github-app
/.claude/*
/concerts/
/thumbnails/
/log/
/node_modules/
/concert-tracker/frontend/node_modules/

/target/
/temp_frames/
/analysis/
/videos/

# PaddleOCR model binaries (~24MB, re-downloadable; see docs/change/2026-06-04-paddle-ocr-evaluation.md)
/live-set-song-splitter/models/

# Vendored ocr-rs build cache: prebuilt MNN downloaded by its build.rs at build time (~22MB)
/live-set-song-splitter/vendor/ocr-rs/3rd_party/
/live-set-song-splitter/vendor/ocr-rs/models/
/live-set-song-splitter/vendor/ocr-rs/target/

# Local-only reference clone of the foldkit framework source (not a build
# dependency - the npm `foldkit` package is). Not committed; re-fetch with
# `git clone https://github.com/foldkit/foldkit.git repos/foldkit` if needed.
/repos/foldkit/

concerts.db
concerts.db-wal
concerts.db-shm

/e2e/.fixture/
/test-results/
/playwright-report/

.DS_Store
