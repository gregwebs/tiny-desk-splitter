@AGENTS.md

## Shell commands

Use Grep/Glob/Read tools rather than creating ad-hoc bash commands.
Use allow listed commands from skills and settings.

## Spawning Subagents

For any read only tasks, spawn subagents using /codex:rescue rather than a normal claude subagent spawn.
This includes using the /code-review skill.
